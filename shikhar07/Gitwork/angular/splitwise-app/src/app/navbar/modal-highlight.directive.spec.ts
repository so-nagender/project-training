import { ModalHighlightDirective } from './modal-highlight.directive';

describe('ModalHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalHighlightDirective();
    expect(directive).toBeTruthy();
  });
});

import { ListEditDirective } from './list-edit.directive';

describe('ListEditDirective', () => {
  it('should create an instance', () => {
    const directive = new ListEditDirective();
    expect(directive).toBeTruthy();
  });
});

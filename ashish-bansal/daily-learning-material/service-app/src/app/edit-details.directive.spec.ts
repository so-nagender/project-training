import { EditDetailsDirective } from './edit-details.directive';

describe('EditDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new EditDetailsDirective();
    expect(directive).toBeTruthy();
  });
});

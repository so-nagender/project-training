import { FirstDirDirective } from './first-dir.directive';

describe('FirstDirDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstDirDirective();
    expect(directive).toBeTruthy();
  });
});

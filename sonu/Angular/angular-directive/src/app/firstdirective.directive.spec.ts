import { FirstdirectiveDirective } from './firstdirective.directive';

describe('FirstdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
